import { FullIndex } from "../../data-index";
import { App, Component, Vault } from "obsidian";
import { Query } from "../../query/query";
import { DataviewSettings } from "../../settings";
import { DataviewRefreshableRenderer } from "../refreshable-view";
export declare class DataviewTaskRenderer extends DataviewRefreshableRenderer {
    query: Query;
    container: HTMLElement;
    index: FullIndex;
    origin: string;
    vault: Vault;
    settings: DataviewSettings;
    app: App;
    taskBindings?: Component;
    constructor(query: Query, container: HTMLElement, index: FullIndex, origin: string, vault: Vault, settings: DataviewSettings, app: App);
    render(): Promise<void>;
}
