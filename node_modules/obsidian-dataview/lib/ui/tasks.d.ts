import { Vault, Component } from "obsidian";
import { Grouping } from "../data-model/value";
import { QuerySettings } from "../settings";
import { SListItem } from "../data-model/serialized/markdown";
/**
 * Render a task grouping (indenting nested groupings for clarity). This will automatically bind the tasks to be checkable,
 * which requires access to a vault.
 */
export declare function renderTasks(container: HTMLElement, tasks: Grouping<SListItem>, originFile: string, component: Component, vault: Vault, settings: QuerySettings): Promise<void>;
