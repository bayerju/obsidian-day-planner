import { FullIndex } from "../../data-index";
import { App } from "obsidian";
import { Query } from "../../query/query";
import { DataviewSettings } from "../../settings";
import { DataviewRefreshableRenderer } from "../refreshable-view";
/** Renders a list dataview for the given query. */
export declare class DataviewListRenderer extends DataviewRefreshableRenderer {
    query: Query;
    container: HTMLElement;
    index: FullIndex;
    origin: string;
    settings: DataviewSettings;
    app: App;
    constructor(query: Query, container: HTMLElement, index: FullIndex, origin: string, settings: DataviewSettings, app: App);
    render(): Promise<void>;
}
