/** Actual import implementation backend. This must remain separate from `import-entry` since it is used without web workers. */
import { ParsedMarkdown } from "../parse/markdown-file";
import { CachedMetadata } from "obsidian";
export declare function runImport(path: string, contents: string, metadata: CachedMetadata): ParsedMarkdown;
